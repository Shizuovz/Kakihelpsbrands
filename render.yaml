services:
  - type: web
    name: kaki-hoardings-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: "npm install"
    startCommand: "node index.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001

